# ManuscriptStepsSidebar: Component Design

---

## 1. Diagrams

### 1.1 Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                     ManuscriptStepsSidebar                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Manuscript Steps (header)               │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  StepItem (Brief)         [collapsed]                  >  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  StepItem (Draft)         [collapsed]                  >  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  StepItem (Script)        [collapsed]                  >  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  StepItem (Prose Dummy)   [expanded]                   ∨  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  PromptPanel (inline when expanded)                 │  │  │
│  │  │  • PROMPT textarea                                  │  │  │
│  │  │  • [Generate] button                                │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  StepItem (Poetry Dummy)  [collapsed]                  >  │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  StepItem (Finalization)  [collapsed]                  >  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 Data Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                     ManuscriptStepsSidebar                        │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Props: activeStep, promptInput, isGenerating,             │  │
│  │         selectedDummyType                                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│         │                                                        │
│         ▼                                                        │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  StepItem[] (rendered via STEPS_CONFIG map)                 │ │
│  │                                                             │ │
│  │  Props per item:                                            │ │
│  │  • step (from config)                                       │ │
│  │  • isActive (activeStep === step.id)                        │ │
│  │  • promptInput (passed when active)                         │ │
│  │  • isGenerating                                             │ │
│  │  • selectedDummyType (for finalization only)                │ │
│  │                                                             │ │
│  │  Callbacks:                                                 │ │
│  │  • onStepClick → onStepChange                               │ │
│  │  • onPromptChange                                           │ │
│  │  • onGenerate                                               │ │
│  │  • onDummyTypeChange (finalization only)                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 1.3 Steps Configuration

| Step ID | Label | Icon | Has PromptPanel | Extra Fields |
|---------|-------|------|-----------------|--------------|
| `brief` | Brief | 📄 | ✅ | - |
| `draft` | Draft | 📄 | ✅ | - |
| `script` | Script | 📄 | ✅ | - |
| `prose_dummy` | Prose Dummy | ▦ | ✅ | - |
| `poetry_dummy` | Poetry Dummy | ▦ | ✅ | - |
| `finalization` | Finalization | ⊛ | ✅ | TYPE selector |

---

## 2. Root Component Design

### 2.1 Overview

**Mục đích:** Left sidebar navigation cho manuscript workflow. Hiển thị danh sách steps, mỗi step có thể expand để hiện PromptPanel cho AI generation.

**Shared Types:**

```typescript
type ManuscriptStepType = 'brief' | 'draft' | 'script' | 'prose_dummy' | 'poetry_dummy' | 'finalization';

type DummyType = 'prose' | 'poetry';

interface StepConfig {
  id: ManuscriptStepType;
  label: string;
  icon: 'doc' | 'grid' | 'finalize';
  showDummyTypeSelector: boolean;
}

const STEPS_CONFIG: StepConfig[] = [
  { id: 'brief', label: 'Brief', icon: 'doc', showDummyTypeSelector: false },
  { id: 'draft', label: 'Draft', icon: 'doc', showDummyTypeSelector: false },
  { id: 'script', label: 'Script', icon: 'doc', showDummyTypeSelector: false },
  { id: 'prose_dummy', label: 'Prose Dummy', icon: 'grid', showDummyTypeSelector: false },
  { id: 'poetry_dummy', label: 'Poetry Dummy', icon: 'grid', showDummyTypeSelector: false },
  { id: 'finalization', label: 'Finalization', icon: 'finalize', showDummyTypeSelector: true },
];
```

### 2.2 Interface

```typescript
interface ManuscriptStepsSidebarProps {
  activeStep: ManuscriptStepType;
  promptInput: string;
  isGenerating: boolean;
  selectedDummyType: DummyType;
  onStepChange: (step: ManuscriptStepType) => void;
  onPromptChange: (prompt: string) => void;
  onGenerate: () => void;
  onDummyTypeChange: (type: DummyType) => void;
}
```

### 2.3 Render Logic (pseudo)

```
ManuscriptStepsSidebar:
  RENDER header "Manuscript Steps"

  FOR EACH step IN STEPS_CONFIG:
    isActive = activeStep === step.id

    RENDER StepItem với:
      - step config
      - isActive
      - IF isActive:
          - promptInput
          - isGenerating
          - selectedDummyType (if step.showDummyTypeSelector)
      - callbacks: onStepClick, onPromptChange, onGenerate, onDummyTypeChange
```

### 2.4 Visual

**Collapsed State (step không active):**

```
┌──────────────────────────────────┐
│ Manuscript Steps                  │
├──────────────────────────────────┤
│ 📄 Brief                       > │
├──────────────────────────────────┤
│ 📄 Draft                       > │
├──────────────────────────────────┤
│ 📄 Script                      > │
├──────────────────────────────────┤
│ ▦  Prose Dummy                 > │
├──────────────────────────────────┤
│ ▦  Poetry Dummy                > │
├──────────────────────────────────┤
│ ⊛  Finalization                > │
└──────────────────────────────────┘
```

**Expanded State (step active - Brief/Draft/Script/Dummy):**

```
┌──────────────────────────────────┐
│ Manuscript Steps                  │
├──────────────────────────────────┤
│ 📄 Brief                       ∨ │
│  ┌────────────────────────────┐  │
│  │ PROMPT                     │  │
│  │ ┌────────────────────────┐ │  │
│  │ │ Enter your prompt...   │ │  │
│  │ │                        │ │  │
│  │ └────────────────────────┘ │  │
│  │ ┌────────────────────────┐ │  │
│  │ │    ✨ Generate         │ │  │
│  │ └────────────────────────┘ │  │
│  └────────────────────────────┘  │
├──────────────────────────────────┤
│ 📄 Draft                       > │
└──────────────────────────────────┘
```

**Expanded State (Finalization - has TYPE selector):**

```
┌──────────────────────────────────┐
│ ⊛  Finalization                ∨ │
│  ┌────────────────────────────┐  │
│  │ TYPE                       │  │
│  │ ┌────────────────────────┐ │  │
│  │ │ Prose                ∨ │ │  │
│  │ └────────────────────────┘ │  │
│  │ PROMPT                     │  │
│  │ ┌────────────────────────┐ │  │
│  │ │ Enter your prompt...   │ │  │
│  │ └────────────────────────┘ │  │
│  │ ┌────────────────────────┐ │  │
│  │ │ ⊛ Generate Art Direction│ │  │
│  │ └────────────────────────┘ │  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

**Generating State:**

```
┌────────────────────────────────┐
│ ┌────────────────────────────┐ │
│ │ ⏳ Generating...           │ │  ← Button disabled, show spinner
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

---

## 3. Child Components Interface

### 3.1 StepItem

📄 **Doc:** [03-01-01-step-item.md](component/editor-page/03-01-01-step-item.md)

**Mục đích:** Single step row với collapsible PromptPanel. Hiển thị step header và expand/collapse để hiện prompt input.

**Props & Callbacks:**

```typescript
interface StepItemProps {
  step: StepConfig;
  isActive: boolean;
  promptInput: string;
  isGenerating: boolean;
  selectedDummyType: DummyType;
  onStepClick: () => void;
  onPromptChange: (prompt: string) => void;
  onGenerate: () => void;
  onDummyTypeChange: (type: DummyType) => void;
}
```

**Visual:**

```
Collapsed:
┌──────────────────────────────────┐
│ 📄 Brief                       > │  ← Click to expand
└──────────────────────────────────┘

Expanded:
┌──────────────────────────────────┐
│ 📄 Brief                       ∨ │  ← Click to collapse
│  ┌────────────────────────────┐  │
│  │     [PromptPanel]          │  │  ← Inline PromptPanel
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

---

## 4. Technical Notes

### 4.1 Key Design Decisions

**Single Active Step**
Chỉ một step được expand tại một thời điểm. Khi click step khác, step hiện tại auto collapse. Lý do: Tránh confusion, focus vào task hiện tại.

**PromptPanel Inline trong StepItem**
PromptPanel không phải component riêng ở cuối sidebar mà nằm inline trong StepItem khi expanded. Lý do: Screenshots cho thấy prompt input thuộc về context của step đang active.

**Finalization Step có TYPE Selector**
Chỉ Finalization step có dropdown chọn dummy type (Prose/Poetry). Lý do: User cần chọn source dummy để generate art direction.

**Generate Button Label**
- Steps thường: "Generate"
- Finalization: "Generate Art Direction"

### 4.2 Accessibility

- Step headers có role="button" với aria-expanded
- Keyboard navigation: Tab qua steps, Enter/Space để toggle
- Focus trap trong PromptPanel khi expanded
